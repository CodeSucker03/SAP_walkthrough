<mvc:View
    controllerName="ui5.walkthrough.controller.App"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true">
<!--
The router (configured in manifest.json) needs to know where to place views when a route matches.
The <App id="app"/> in App.view.xml acts as a permanent placeholder.
Without this, every view would have to re-render the whole page structure (Shell, App, etc.), which is inefficient. -->
    <Shell>
        <App
            class="myAppDemoWT"
            id="app"/>
    </Shell>
</mvc:View>
